# VCPToolBox Bug 审查清单（2026-04-14）

本清单基于本次代码静态审查整理，当前**不包含修复**，只记录已确认或高概率成立的问题。  
排序规则按影响范围和故障严重程度从高到低排列。

评级说明：

| 评级 | 含义 |
| --- | --- |
| `P1` | 高优先级。会直接导致核心功能失效、配置误导、审批卡死，或存在明显的数据/安全风险。 |
| `P2` | 中高优先级。会导致协议不兼容、并发错单、特定模型/链路失效。 |
| `P3` | 中优先级。会造成结果错误、调试信息失真、兼容性/可维护性明显下降。 |

| 评级 | 模块 / 位置 | Bug 描述 | 影响 | 证据与判断 |
| --- | --- | --- | --- | --- |
| `P1` | `Plugin.js:782-823`<br>`WebSocketServer.js:290-295`<br>`AdminPanel/js/tool-approval.js:6-60` | **工具人工审批链路在仓库内不可闭环。** 服务端会把 `tool_approval_request` 广播给 `VCPLog` 客户端，但当前内置 AdminPanel 里的“工具审批管理”页面只负责读写配置，没有任何 WebSocket 审批弹窗或 `tool_approval_response` 发送逻辑。 | 一旦开启人工审批，被命中的工具调用会一直卡到超时，等同于把这些工具“软禁用”。 | `Plugin.js` 会等待审批结果后才继续执行；`WebSocketServer.js` 只有接收 `tool_approval_response` 的代码；内置前端 `tool-approval.js` 仅有配置表单，没有审批消息处理逻辑。 |
| `P1` | `routes/admin/config.js:63-72`<br>`server.js:150-166`<br>`server.js:372-375`<br>`routes/specialModelRouter.js:8-12,31-32` | **后台“保存并重新加载主配置”是误导性的。** 接口写完 `config.env` 后只调用 `pluginManager.loadPlugins()`，但主服务大量关键配置在模块加载时就缓存成常量，特殊模型路由也在 require 时就把上游地址、Key、白名单读死了。 | 修改 `API_URL`、`API_Key`、管理账号、端口、模型白名单等后，后台提示“已重新加载”，但运行时仍可能继续使用旧值，直到重启。 | `routes/admin/config.js` 没有重新执行 `dotenv.config({ override: true })`，也没有重建 `server.js`/`specialModelRouter.js` 内的模块级常量。 |
| `P1` | `routes/admin/plugins.js:139-160,232-286,362-363`<br>`Plugin.js:417-603`<br>`server.js:1169-1211` | **后台插件热重载不完整。** 管理接口切换插件或保存插件配置时，只调用 `pluginManager.loadPlugins()`；但 service 路由挂载和 static 插件调度只在启动阶段执行一次。 | 新启用的 service 插件可能不会真正挂上路由；已禁用的 service 路由可能继续暴露；static 插件的定时任务和占位符刷新也可能与磁盘状态脱节。 | `server.js` 中 `initializeServices()` 和 `initializeStaticPlugins()` 只在启动序列执行；而后台所有相关操作都只调用 `loadPlugins()`。 |
| `P1` | `routes/dailyNotesRoutes.js:572-599`<br>`routes/dailyNotesRoutes.js:604-668`<br>`routes/dailyNotesRoutes.js:671-695`<br>`routes/dailyNotesRoutes.js:498-500,558-560` | **日记本写入/移动/删除接口缺少符号链接防护。** 读取路径时做了 `isSymlink()` 检查，但写、移、删三个变更接口只做了 `isPathSafe()`，没有阻止对根目录内符号链接的跟随。 | 只要知识库目录内存在指向外部位置的 symlink，写入、移动、删除就可能落到根目录外，造成数据越权修改或删除。 | 代码对读路径显式阻止 symlink，但对写路径没有同级检查，属于同一安全模型下的明显缺口。 |
| `P2` | `routes/specialModelRouter.js:69-112` | **图像白名单模型的 `/v1/chat/completions` 代理破坏 OpenAI 兼容协议。** 该路由忽略 `stream`，始终 `await apiResponse.json()`；如果上游返回 `candidates`，还会直接把数组裸返回，而不是标准的 `chat.completion` 结构。 | 任何依赖 `choices[0].message` 或 SSE 的客户端，在命中图像白名单模型时都可能直接解析失败。 | 当前实现不是“透传”，而是把上游结构重写成另一种格式；这与项目其余 `/v1/chat/completions` 接口的兼容目标不一致。 |
| `P2` | `routes/dailyNotesRoutes.js:307-321`<br>`routes/dailyNotesRoutes.js:323-349` | **搜索去重和取消信号绑定方式有串扰。** 相同搜索词会复用第一个 inflight promise，但实际执行所用的 `AbortController` 只绑定了首个请求方。 | 第一个客户端断开或取消后，会把后续复用同一 promise 的其他客户端搜索一并中断，形成跨请求误伤。 | 去重逻辑复用了首个 `entry.promise`，但没有为后续复用者建立独立取消语义。 |
| `P2` | `modules/chatCompletionHandler.js:109-112,807-818`<br>`modules/handlers/nonStreamHandler.js:42-45,136-156,254-274` | **Codex 非流式兼容逻辑前后不一致。** 首轮请求会基于“API URL 或模型名”判断是否走 Codex 兼容缓冲；但进入非流式 VCP 递归后，只按 `apiUrl.includes('/codex')` 判断。 | 当 Codex 通过模型别名挂在通用 OpenAI 网关后，首轮可成功，工具回合递归请求却可能退回普通 JSON 路径并失败。 | 初始调用和递归调用使用了两套不同的 Codex 判定标准，属于明确的条件分叉不一致。 |
| `P3` | `vcpInfoHandler.js:117-152`<br>`modules/handlers/streamHandler.js:260-263` | **流式 Archery 工具的 VCP 展示参数顺序写反。** `streamVcpInfo()` 的签名要求 `(modelName, toolName, status)`，但这里传入的是 `(modelName, status, toolName)`。 | SSE 中展示的工具名和成功/失败状态会互相串位，影响调试和日志判读。 | 同文件其余调用点（例如 `modules/handlers/streamHandler.js:368`）使用的是正确顺序，这里明显是单点写错。 |
| `P3` | `Plugin.js:141-142`<br>`Plugin.js:999-1002` | **插件命令启动方式会破坏带空格/引号的命令行。** 代码先用 `split(' ')` 人工拆分命令，再传给 `spawn(..., { shell: true })`。 | 含空格路径、带引号参数、复杂命令行的插件在 Windows 下很容易启动失败，且 manifest 中原始参数边界会被改写。 | 这是典型的 argv 切分错误：命令行引用规则交给 shell 之前就已经被错误拆散。 |

## 备注

- 本轮优先审查了：启动链路、管理后台、插件热重载、聊天编排、VCP 循环、特殊模型路由、分布式相关主干代码。
- 这是**高价值问题清单**，不是“全仓所有小瑕疵穷举表”；如果继续深挖插件子系统，还能再补出更多插件级问题。
- 当前工作区本身已经有未提交改动，这份文档没有改业务代码，只新增审查结果文件。

## 修复状态（2026-04-14）

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| 工具人工审批链路闭环 | `已修复` | 后端新增待处理审批请求 API，AdminPanel 新增待审批列表和批准/拒绝操作，`PluginManager` 也会保留审批上下文用于展示。 |
| 主配置保存后的热加载误导 | `已修复` | 保存 `config.env` 后会重新载入 `process.env`、重建聊天处理器，并返回“哪些配置仍需重启”的真实提示。 |
| 后台插件热重载不完整 | `已修复` | 新增插件运行时重载流程，会重新发现插件、重挂 service 路由、重启 static 调度并刷新依赖注入/表情缓存。 |
| 日记写入/移动/删除 symlink 缺口 | `已修复` | 写、移、删和空目录删除现在都会拦截 symlink 文件夹/文件，避免越界改写。 |
| 特殊图像模型代理破坏 OpenAI 兼容 | `已修复` | 特殊模型路由改成原始响应透传，支持保留非流式 JSON 和 SSE 语义，并改为动态读取当前配置。 |
| 搜索去重与取消串扰 | `已修复` | 相同搜索共享底层任务，但每个调用方的取消信号只会中断自己，不会误伤其他等待者。 |
| Codex 非流式递归判定不一致 | `已修复` | 递归非流式回合同步使用主处理器的 Codex 判定逻辑。 |
| 流式 Archery VCP 展示参数错位 | `已修复` | 修正 `streamVcpInfo()` 调用参数顺序。 |
| 插件命令 `split(' ')` 破坏引号/空格 | `已修复` | 改为整条命令字符串交给 shell 执行，避免 Windows 下带空格路径和带引号参数失真。 |
